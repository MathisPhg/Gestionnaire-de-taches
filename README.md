## Utilisation

- utiliser la DB

telechager la database todolist.sql et l'importer 

- Lier a la DB

Dans connectToDB.php veullez entre l'adresse de la DB dans la variable $dsn ainsi que l'utilisatuer et le mot de passe dans les variable associer