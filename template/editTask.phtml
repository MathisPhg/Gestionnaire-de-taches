<?php
$task = $task ?? [
    'title' => '',
    'content' => '',
    'urgent' => 0,
    'important' => 0
];
?>

<main class="edit-task">

    <form method="POST" class="edit-task-form">

        <h1>Edit Task</h1>

        <!-- title -->
        <div>
            <label for="title">Title</label>
            <input 
                type="text" 
                id="title" 
                name="title" 
                value="<?= htmlspecialchars($task['title']) ?>" 
                required
            >
        </div>

        <!-- content + zone écriture -->
        <div>
            <label for="content">Content</label>
            <textarea 
                id="content" 
                name="content" 
                rows="6" 
                required
            ><?= htmlspecialchars($task['content'] ?? '') ?></textarea>
        </div>

        <!-- switch urgent -->
        <div>
            <label>Urgent</label>
            <label class="switch">
                <input type="checkbox" name="urgent" <?= $task['urgent'] ? 'checked' : '' ?>>
                <span class="slider"></span>
            </label>
        </div>

        <!-- switch important -->
        <div>
            <label>Important</label>
            <label class="switch">
                <input type="checkbox" name="important" <?= $task['important'] ? 'checked' : '' ?>>
                <span class="slider"></span>
            </label>
        </div>

        <!-- btn save bas page -->
        <div>
            <button type="submit">
                Save
            </button>
        </div>

    </form>

</main>
